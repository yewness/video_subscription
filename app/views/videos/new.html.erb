<h3>New Video</h3>

<% if !current_user.nil? %>
	<% if current_user.has_role? :admin %>
		<div class="card">
			<div class="card-content">
				<%= render 'form' %>
			</div>
		</div>
	<% end %>
<% end %>